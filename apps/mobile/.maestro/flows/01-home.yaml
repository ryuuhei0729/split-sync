# ホーム（動画取り込み）画面
# ヒーローメッセージ + 3ステップフロー + 動画選択 CTA。ローカルデータ不要で成立する。
# 必要な env: APP_ID, LOCALE, REGION, OUTPUT_DIR, DEVICE_TAG
appId: ${APP_ID}
---
- runFlow: ../subflows/launch.yaml
- waitForAnimationToEnd:
    timeout: 5000
- takeScreenshot: ${OUTPUT_DIR}/${DEVICE_TAG}_01_home
